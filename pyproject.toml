[project]
name = "szl-os"
version = "0.1.0"
description = "SZL Sovereign OS — fail-closed operator kernel"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.setuptools.packages.find]
include = ["szl_os*"]
